// StructureConfig.hpp
#ifndef STRUCTURECONFIG_HPP
#define STRUCTURECONFIG_HPP

#include <SFML/Graphics.hpp>
#include <string>

struct StructureConfig {
    std::string type;
    sf::Color color;
    int count;
};

#endif // STRUCTURECONFIG_HPP